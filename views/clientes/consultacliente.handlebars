
{{!-- função que exibe os possíveis alertas de erros --}}
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
{{#if user}}
<div class="card mt-2">
    <div class="card-body">
        <h1>Consulta clientes</h1>
    </div>
</div>

{{!-- formulário de consulta do cliente --}}
<div class="card mt-2">
    <div class="card-body">
        <input type="hidden" name="id" value="{{clientes._id}}">
            <label for="nome">Nome: {{clientes.nome}}</label>
            <h1></h1>
            <label for="fone">Telefone: {{clientes.telefone}}</label>
            <h1></h1>
            <label for="email">Email: {{clientes.email}}</label>
        </form>
    </div>
</div>


{{else}}

{{/if}}