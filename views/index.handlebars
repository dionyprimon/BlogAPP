<div class="p-3 mb-4 bg-light rounded-3" >
    <div class="container-fluid py-2">
        <h1 class="display-5 fw-bold">Bem vindo ao BlogNODE</h1>
        <p class="col-md-20 fs-5">Este blog e suas funções são para demonstrar os conhecimentos adquiridos por video aulas de Node.JS + Express + MongoDB</p>
        <form action="/usuarios/registro">
            <button type="submit" class="btn btn-primary btn-lg">Crie uma conta</button>
        </form>
    </div>
</div>
<hr>
<h2>Postagens recentes</h2>
{{#each postagens}}
    <div class="card mt-2">
        <div class="card-body">
            <h4>Título: {{this.titulo}}</h4>
            <small>Descrição: {{this.descricao}}</small><br>
            <a href="/postagem/{{this.slug}}" ><button class="btn btn-dark mt-2">Leia mais</button></a>
            <hr>
            <small>Categoria: {{this.categoria.nome}}</small><br>
            <small>Data: {{this.data}}</small><br>
        </div>    
    </div>
{{else}}
    <h4 class="mt-3">Nenhum post cadastrado</h4>
{{/each}}