
{{!-- função que exibe os possíveis alertas de erros --}}
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
{{#if user}}
<div class="card mt-2">
    <div class="card-body">
        <h1>Ficha de cadastro</h1>
    </div>
</div>

{{!-- formulário de cadastro do usuário --}}
<div class="card mt-2">
    <div class="card-body">

            <label for="nome">Nome: {{produtos.nome}}</label>
            <h1></h1>
            <label for="valor">Valor unitário: {{produtos.valor}}</label>
            <h1></h1>       

            {{!-- mostra o nome da categoria (relacionando o COD da categoria cadastrada no item com o campo nome na tabela categoria) --}}
            {{#each categorias}}
            <label for="categoria" value="{{_id}}">Categoria: {{nome}}</label>
            {{else}}
                    <option value="0">Nenhuma categoria cadastrada</option>
            {{/each}}

            <h1></h1>
            <label for="categoria">COD Categoria: {{produtos.categoria}}</label>
            <h1></h1>

        </form>
    </div>
</div>

{{else}}

{{/if}}